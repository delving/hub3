package hextuples

import (
	"io"

	"github.com/delving/hub3/ikuzo/rdf"
)

func Parse(r io.Reader, g *rdf.Graph) (*rdf.Graph, error) {
	// parse lines
	// decode HexTuple
	// Add to graph
	return nil, nil
}
